I will implement a robust internationalization (i18n) system for the frontend as requested.

### 1. Dependencies & Setup
- **Install `vue-i18n`**: Add the standard Vue i18n library to `package.json`.
- **Initialize i18n**: Create `src/i18n.ts` to configure the i18n instance with English (default) and Chinese support.
- **Main Entry**: Update `src/main.ts` to use the i18n plugin.

### 2. State Management (Pinia)
- **Create `src/stores/locale.ts`**: 
  - Manage the current language state (`'en' | 'zh'`).
  - Persist the user's choice to `localStorage` so it survives page reloads.
  - Provide actions to switch languages, which will update both the Pinia state and the `i18n` instance locale.

### 3. Translation Resources
- **Create `src/locales/en.ts`**: Move all hardcoded English strings here.
- **Create `src/locales/zh.ts`**: Provide Chinese translations for the same keys.
- **Structure**:
  - `common`: General terms (Settings, Save, Cancel).
  - `nav`: Sidebar navigation items (Overview, Sensor, Data).
  - `settings`: specific strings for the Settings modal (Serial, Server, Debug Mode, etc.).

### 4. Component Implementation
- **`src/components/SettingsModal.vue`**:
  - Add a "Language" / "语言" selection section (Dropdown or Toggle).
  - Replace hardcoded titles ("Settings", "Serial Communication") and labels with `$t(...)`.
- **`src/components/layout/Sidebar.vue`**:
  - Update menu items to use translated labels.
  - Update tooltips to use translated strings.

### 5. Verification
- **Test**: Verify that switching the language in Settings updates the UI immediately and persists after refresh.

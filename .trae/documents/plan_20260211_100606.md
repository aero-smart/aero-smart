我已经完成了所有修复和国际化工作。

### 主要改动
1.  **修复遗漏的翻译**:
    -   在 `DashboardView.vue` 中找到了用户指出的 "Wind Input", "Pressure Difference Monitor" 以及其他遗漏的硬编码字符串（如 "Wind Speed Display", "Wind Direction" 等），并全部替换为 `$t(...)` 调用。
2.  **修复代码逻辑**:
    -   检查并修复了 `PowerView.vue` 中的一个 Bug：在 `watch` 函数中访问 `availableSources` 时缺少了 `.value`，这会导致运行时错误。
    -   确认 `ControlView.vue` 的代码逻辑正确，所有 `computed` 属性的使用都正确带上了 `.value`。
3.  **完善设置页面**:
    -   更新了 `SettingsModal.vue`，将硬编码的 `alert` 和 `confirm` 消息替换为国际化字符串。
4.  **验证**:
    -   使用 `grep` 搜索确认了所有关键的硬编码英文字符串（如 "Wind Input"）现在只存在于注释和翻译文件中，视图代码中已清除干净。

### 系统概览
-   **语言**: 支持 英文 (默认) 和 中文。
-   **状态管理**: 使用 Pinia 持久化存储用户语言偏好。
-   **响应式**: 所有图表配置、传感器列表和建议内容现在都是响应式的，切换语言时会立即更新，无需刷新页面。

现在您可以启动开发服务器并测试语言切换功能。所有界面元素，包括图表标题和动态生成的列表，都应该能正确显示中文。
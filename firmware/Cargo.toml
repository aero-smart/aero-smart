# This file was automatically generated.

[package]
edition = "2024"
name = "firmware"
version = "0.1.0"

[dependencies]
cortex-m = { version = "0.7.7", features = [
  "inline-asm",
  "critical-section-single-core",
] }
cortex-m-rt = "0.7.5"
defmt = { version = "1.0.1" }
defmt-rtt = { version = "1.1.0" }
embassy-executor = { version = "0.9.1", features = [
  "arch-cortex-m",
  "executor-thread",
  "defmt",
] }
embassy-futures = "0.1.2"
embassy-stm32 = { version = "0.4.0", features = [
  "memory-x",
  "stm32h743vg",
  "time-driver-tim4",
  "exti",
  "unstable-pac",
  "defmt",
  "chrono",
] }
embassy-sync = { version = "0.7.2", features = ["defmt"] }
embassy-time = { version = "0.5.0", features = [
  "defmt",
  "defmt-timestamp-uptime",
  "tick-hz-48_000_000",
] }
heapless = { version = "0.8", default-features = false, features = [
  "defmt-03",
] }
nb = "1.0.0"
panic-halt = "1.0.0"
panic-probe = { version = "1.0.0", features = ["print-defmt"] }
aerosmart-shared = { path = "../shared", default-features = false, features = [
  "defmt",
] }
dshot-frame = "0.2.1"
pid = "4.0.0"
num-traits = { version = "0.2.19", default-features = false, features = [
  "libm",
] }
bme280-rs = "0.3.0"
ws2812-async = "0.3.0"
smart-leds-trait = "0.3.2"
smart-leds = "0.4.0"
ahrs = { version = "0.8.0", default-features = false }

[[bin]]
name = "firmware"
test = false
bench = false

[profile.dev]
debug = true
lto = true
opt-level = "z"
incremental = true

[profile.release]
debug = false
lto = true
opt-level = "z"
incremental = true

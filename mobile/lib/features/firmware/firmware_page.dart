import 'package:flutter/material.dart';

class FirmwarePage extends StatelessWidget {
  const FirmwarePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Firmware Update'));
  }
}

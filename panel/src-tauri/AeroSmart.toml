[serial]
port = "/dev/tty.usbmodem1234"
baud_rate = 915200
handshake_timeout_secs = 10
retry_interval_secs = 5

[server]
port = 3000
host = "0.0.0.0"

[rules]
debug_mode = true
enable_onboarding = false
